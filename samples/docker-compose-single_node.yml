services:
  fasttask:
    image: docker.io/irid/fasttask:test
    container_name: fasttask
    restart: always

    ports:
      # 映射api端口 
      - "9001:443"

    volumes:
      - ./files:/fasttask/files

    environment:
      # single_node: 单一节点提供接口+执行任务
      - NODE_TYPE=single_node
      - FLOWER_ENABLED=True
      # 结果存储：AUTO = 序列化后超过 1MB 的结果自动外置到内嵌对象存储，
      # 小结果仍内联在 Redis 中（改回 JSON 可恢复历史行为）
      - RESULT_TYPE=AUTO
